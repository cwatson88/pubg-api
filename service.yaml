apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
name: pubg-rust-server
namespace: default
spec:
runLatest:
  configuration:
  revisionTemplate:
    spec:
    container:
      image: docker.io/cwatson1988/ pubg-rust-server
      env:
        - name: TARGET
      value: "Rust Sample v1"